# DISABLED EXAMPLE — validation must pass before scheduling.
# 30 18 * * 1-5 cd /path/to/curvelens-core && CCVM_PRODUCT=sp500 \
#   openclaw agent --message 'Use $curvelens-daily-analysis to run S&P 500 for today; do not deliver.'
